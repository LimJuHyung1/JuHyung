
def add(alpha: int, beta: int) -> int:
    return alpha + beta

def sub(alpha: int, beta: int) -> int:
    return alpha - beta

def mut(alpha: int, beta: int) -> int:
    return alpha * beta

def div(alpha: int, beta: int) -> int:
    return alpha // beta, alpha % beta


